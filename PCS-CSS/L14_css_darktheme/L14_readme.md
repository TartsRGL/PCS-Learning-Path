# Lesson 14 – CSS Dark Theme & Custom Properties

This folder contains the HTML code for **Lesson 14** of the *PCScss* series.

It demonstrates:

- The use of **CSS custom properties** (variables) for colors and fonts.
- Light and dark theme switching using `prefers-color-scheme`.
- Applying variables across layout elements.
- Providing **fallback values** for older browsers.
- Implementing a **manual theme toggle** via JavaScript.

Each commit adds one conceptual layer step-by-step.

---

## 📌 Commits overview

```bash
git commit -m "feat: Add basic structure"
git commit -m "feat: Define color variables and dark mode via media query"
git commit -m "feat: Apply variables to layout (bg, text, links, spacing)"
git commit -m "feat: Add fallback values for legacy browser support"
git commit -m "feat: Add JS theme toggle with .dark-theme class"
git commit -m "docs: Add README.md with commit log and learning goals"
```

---

📁 [Back to repo: PCS-CSS](https://github.com/TartsRGL/PCS-CSS)

---

## 🙏 Acknowledgment

Special thanks to [Praha Coding School](https://prahacoding.cz/) and  
[Mr. Wernerdweight](https://github.com/wernerdweight) for their guidance and inspiration.
