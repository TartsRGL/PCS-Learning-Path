# Lesson 15 – CSS Grid Layout in Practice

This folder contains the HTML code for **Lesson 15** of the *PCScss* series.

It demonstrates:

- The use of **CSS Grid layout** with `auto-fit`, `minmax()`, and `fr` units.
- Building both fixed and responsive **gallery/dashboard layouts**.
- Applying layout control via `grid-template-areas`.
- Styling content blocks like `.hero`, `.note`, and `.item`.
- Using **CSS variables** (`:root`) for unified spacing and color logic.

Each commit adds one conceptual layer step-by-step.

---

## 📌 Commits overview

```bash
git commit -m "feat: Add basic structure"
git commit -m "feat: Enable grid layout with 3 columns"
git commit -m "feat: Add layout map via grid-template-areas"
git commit -m "feat: Switch to responsive layout with auto-fit + minmax()"
git commit -m "style: Add visual styling (color, padding, radius)"
git commit -m "feat: Add hero section and note block"
git commit -m "chore: Cleanup and centralize variables"
git commit -m "style: Add spacing under hero"
git commit -m "docs: Add README.md with commit log and learning goals"
```

---

📁 [Back to repo: PCS-CSS](https://github.com/TartsRGL/PCS-CSS)

---

## 🙏 Acknowledgment

Special thanks to [Praha Coding School](https://prahacoding.cz/) and  
[Mr. Wernerdweight](https://github.com/wernerdweight) for their guidance and inspiration.