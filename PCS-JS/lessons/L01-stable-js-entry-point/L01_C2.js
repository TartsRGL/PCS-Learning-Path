/* Commit 1 – Define stable JavaScript entry point */
/* Commit 2 – Structure stable initialization flow */

document.addEventListener('DOMContentLoaded', function () {
  initApp();
});

function initApp() {
  prepareApp();
}

function prepareApp() {
  // Stable initialization layer for future lessons.
}
